### To run the example01 file
1. Open run01.sh file. Set the FASTJET_CONFIG to the fastjet-config file. (e.g. FASTJET_CONFIG=/path/to/fastjet/bin/fastjet-config)
2. Run run01.sh file with `./run01.sh` command.
